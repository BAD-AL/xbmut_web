name: xbmut_web
description: An absolute bare-bones web app.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  web: ^1.1.1
  xbox_memory_unit_tool:
    git:
      url: https://github.com/BAD-AL/xbox_memory_unit_tool
      ref: main

dev_dependencies:
  build_runner: ^2.4.15
  build_web_compilers: ^4.1.6
  lints: ^6.0.0
